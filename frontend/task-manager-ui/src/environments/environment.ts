export const environment = {
  production: false,
  // When using proxy.conf.json, /api is routed to http://localhost:5000/api
  // We provide direct fallback to http://localhost:5000/api if needed
  apiUrl: '/api'
};
